kind: Service
apiVersion: v1
metadata:
  name: nginx-service2
  namespace: nginx
spec:
  selector:
    app: nginx2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP #Cluster IP, Node Port, Load Balance

